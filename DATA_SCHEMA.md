# Data Schema

recordings.csv — one row per recording
credits.csv — one row per recording/artist/role credit
relationships.csv — one row per graph relationship

Recording metadata belongs on recordings, not relationships.

target_recording_id remains blank until related recordings are independently resolved.
